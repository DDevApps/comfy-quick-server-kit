[Unit]
Description=ComfyUI crash notifier
After=network.target

[Service]
Type=oneshot
User={{USER_NAME}}
Environment=HOME=/home/{{USER_NAME}}
EnvironmentFile={{PROJECT_ROOT}}/.env
ExecStart={{PROJECT_ROOT}}/scripts/notify-crash.sh